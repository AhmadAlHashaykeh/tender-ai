# Copy to hostinger.env on the server (never commit hostinger.env).
# Used by deploy/hostinger-deploy.sh

APP_URL=https://ahmadalhashaykeh.com/tenderai
VITE_BASE_PATH=/tenderai/public/

DB_DATABASE=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_database_password

# Optional overrides (Hostinger defaults shown)
DB_HOST=127.0.0.1
DB_PORT=3306
