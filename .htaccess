<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all requests into Laravel's public/ directory (subfolder install).
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
